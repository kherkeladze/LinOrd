
times:
   element: 0.25
   before_relation: 0.25
   relation: 0.25
   after_relation: 0.25
   after_pair: [2.5, 3]
   after_last_pair: [5, 5.5]
   pre_question_mark: 0.1
   question_mark: 0.2
   response: 3.5

sizes:
   letter: 2.5
   fix_height: 0.25
   fix_width: 0.1

fix_shape: 'circle'
fix_between_pairs: True
fix_during_wait: True

# keys for yes/no (mapping is randomized)
resp_keys: [f, j]

# wybór triali
# ------------
# repetitions mówi o tym ile razy bierzemy podstawowe kombinacje
# jeżeli używamy 2 trudności * 2 kolejności modeli
# a do każdego mamy 3 odległości pytań * 2 czyodwrócona relacja * 2 czyodpowiedź tak
# to mamy 4 warunki a do każdego 4 pakiety pytań (po 3)
# w związku z tym mamy 4 * 4 = 16 kombinacji warunek - pytania
# te wartość mnożymy przez repetitions i mamy docelową liczbę triali
repetitions: 2
condition_rows: [0, 2] # tzn pierwszy i trzeci rząd (czyli bez środkowych)
condition_columns: [0, 1] # obie sekwencje dla każdego warunku

przerwa_co_ile_modeli: 1
tekst_przerwy: Czas na przerwę - jeżeli masz taką potrzebę.\nMożesz teraz mrugać i ruszać się do woli.\nAby przejść dalej naciśnij dowolny klawisz.

quit:
   enable: True
   button: q